- figure out this connectoin.end() issue

Various manipulations either get or post should only be on the currently showing files:
  - search the filtered and filter the searched
  - clear all only the files currently showing
